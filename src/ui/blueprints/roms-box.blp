using Gtk 4.0;
using Adw 1;

template $RomsBox: Gtk.ListBox {
  styles [
    "boxed-list",
  ] 

  valign: start;
  selection-mode: none;
  margin-top: 10;
  margin-bottom: 10;
  margin-start: 10;
  margin-end: 10;
}
